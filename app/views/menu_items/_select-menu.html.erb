<%= form_with url: menu_items_path, method: "GET" do |form| %>
  <%= select_tag "current_menu_id",
                 options_from_collection_for_select(Menu.all, "id", "menu_name", menu_id),
                 :onchange => "submit()" %>
<% end %>
